import { caseStudyMetadata } from '../../../../lib/case-study-metadata';

export const metadata = caseStudyMetadata('indstrz', 'ar');

export default function ArabicCaseStudyLayout({ children }) {
  return children;
}
