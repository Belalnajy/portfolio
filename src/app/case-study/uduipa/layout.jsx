import { caseStudyMetadata } from '../../../lib/case-study-metadata';

export const metadata = caseStudyMetadata('uduipa', 'en');

export default function CaseStudyLayout({ children }) {
  return children;
}
